h2
  | {{ :: 'credit_cards.credit_cards' | translate}}

#credit-cards
  .not-found(ng-if='creditCards.length == 0')
    p
      | {{ :: 'credit_cards.no_credit_cards_on_file' | translate}}

  .found.clearfix(ng-repeat="card in creditCards")
    credit-card-view(card='card' allow-delete='true')
